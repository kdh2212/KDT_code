package se01.example;

public class OrderEx {

	public static void main(String[] args) {
		Order order = new Order();
		

		order.printStatus();
		order.nextStatus();
		order.printStatus();
		order.nextStatus();
		order.printStatus();
		order.nextStatus();
		order.printStatus();
		order.nextStatus();
		order.printStatus();
	}

}
